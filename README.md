# dissonia
Ambitious crates for muxing and encoding of audio formats (kinda inspired by symphonia in structure)

Currently paused, as i have enough functionality for most cases (opus should be the standard moving forward), so is in maintainance mode for other formats, and feature requests (if applicable) would only be recommended for opus (unless you'd like to PR another format or codec)
